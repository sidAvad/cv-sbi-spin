sync:
  cv-sbi-spin-adamant:
    alpha: "."
    beta: "sa4604@adamant:~/cv-sbi-spin"
    mode: "two-way-resolved"
    ignore:
      paths:
        - ".git"
        - ".venv"
        - "__pycache__"
        - "*.pyc"
        - "outputs/"
        - "dry-runs/"
        - "*.log"
        - ".DS_Store"
        - "nohup.out"
        - "notebooks/"
